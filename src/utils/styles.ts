import { COLORS_DARK_THEME } from "theme/colors";

export const styles = {
    assetsIconHolder: {
        padding: '10px 25px', 
        marginTop: '3px',
        marginBottom: '10px', 
        display: 'flex', 
        height: '50%', 
        alignItems: 'center', 
        borderRadius: '10px', 
        width: '100%',
    }

} as const