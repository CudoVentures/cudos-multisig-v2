import { COLORS_DARK_THEME } from "theme/colors";

export const styles = {
    links: {
        color: COLORS_DARK_THEME.PRIMARY_BLUE, 
        textDecoration: 'none', 
        marginTop: '5px'
    }
} as const